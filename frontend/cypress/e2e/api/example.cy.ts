describe('API Tests', () => {
    it('should return 200 for a valid endpoint', () => {
        // Replace with a valid API endpoint from your backend
        // Assuming backend is running on localhost:8000
        // cy.request('http://localhost:8000/api/health').then((response) => {
        //   expect(response.status).to.eq(200);
        // });
        cy.log('Configure API base URL in cypress.config.ts or use absolute URLs');
    });
});
